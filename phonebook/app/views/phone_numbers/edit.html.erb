<%= link_to 'Back', contact_path(@contact.id) , class: "badge badge-info" %>
<h4 class="card-title"><%= "Editing Contact Number for #{@contact.first_name}" %></h4>

<%= render 'form', phone_number: @phone_number %>


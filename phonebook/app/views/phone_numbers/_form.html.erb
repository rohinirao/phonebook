<%= form_with(model: [@contact,phone_number], local: true) do |form| %>
  <% if phone_number.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(phone_number.errors.count, "error") %> prohibited this contact number from being saved:</h2>

      <ul>
      <% phone_number.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
   <div class="form-group row">
    <%= form.label :label  , class: "col-sm-2 col-form-label"%>
    <div class="col-sm-10">
      <%= form.text_field :label, id: :phone_number_label , class: "form-control input-width"%>
    </div>
  </div>

   <div class="form-group row">
    <%= form.label :phone_number  , class: "col-sm-2 col-form-label"%>
    <div class="col-sm-10">
      <%= form.text_field :phone_number, id: :phone_number_phone_number , class: "form-control input-width"%>
    </div>
  </div>

  <div class="actions">
    <%= form.submit "Save", class: "btn btn-primary" %>
  </div>
<% end %>
